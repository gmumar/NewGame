package throwaway;


public class TriangleCar {
	
	public static String makeSomething() {

		System.out.println("trash");
		
		return null;
		/*JSONParent car = new JSONParent();

		// component list
		ArrayList<JSONComponent> parts = new ArrayList<JSONComponent>();
		JSONComponent part1 = new JSONComponent();
		part1.setComponentName(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "0");
		parts.add(part1);

		JSONComponent part2 = new JSONComponent();
		part2.setComponentName(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "1");
		HashMap<String, String> prop2 = new HashMap<String, String>();
		prop2.put(Properties.ROTATION.name(), "90");
		prop2.put(Properties.POSITION.name(), "-10,5");
		part2.setProperties(prop2);
		parts.add(part2);

		JSONComponent part3 = new JSONComponent();
		part3.setComponentName(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "2");
		HashMap<String, String> prop3 = new HashMap<String, String>();
		prop3.put(Properties.ROTATION.name(), "90");
		prop3.put(Properties.POSITION.name(), "10,5");
		part3.setProperties(prop3);
		parts.add(part3);

		JSONComponent part4 = new JSONComponent();
		part4.setComponentName(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "0");
		HashMap<String, String> prop4 = new HashMap<String, String>();
		prop4.put(Properties.MOTOR.name(), "1");
		part4.setProperties(prop4);
		parts.add(part4);

		JSONComponent part5 = new JSONComponent();
		part5.setComponentName(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "1");
		HashMap<String, String> prop5 = new HashMap<String, String>();
		prop5.put(Properties.MOTOR.name(), "1");
		part5.setProperties(prop5);
		parts.add(part5);

		JSONComponent part6 = new JSONComponent();
		part6.setComponentName(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "2");
		HashMap<String, String> prop6 = new HashMap<String, String>();
		prop6.put(Properties.MOTOR.name(), "1");
		part6.setProperties(prop6);
		
		parts.add(part6);
		car.setComponentList(parts);

		// joint list
		ArrayList<JSONJoint> joints = new ArrayList<JSONJoint>();
		JSONJoint joint1 = new JSONJoint();
		joint1.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "0"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joint1.setMount2(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "1"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joints.add(joint1);

		JSONJoint joint2 = new JSONJoint();
		joint2.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "1"
				+ Assembler.NAME_MOUNT_SPLIT + "2");
		joint2.setMount2(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "2"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joints.add(joint2);

		JSONJoint joint3 = new JSONJoint();
		joint3.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "2"
				+ Assembler.NAME_MOUNT_SPLIT + "2");
		joint3.setMount2(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "0"
				+ Assembler.NAME_MOUNT_SPLIT + "2");
		joints.add(joint3);
		
		JSONJoint joint4 = new JSONJoint();
		joint4.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "2"
				+ Assembler.NAME_MOUNT_SPLIT + "2");
		joint4.setMount2(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "0"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joints.add(joint4);

		JSONJoint joint5 = new JSONJoint();
		joint5.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "2"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joint5.setMount2(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "1"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joints.add(joint5);

		JSONJoint joint6 = new JSONJoint();
		joint6.setMount1(ComponentNames._BAR3_.name() + Assembler.NAME_ID_SPLIT + "0"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joint6.setMount2(ComponentNames._TIRE_.name() + Assembler.NAME_ID_SPLIT + "2"
				+ Assembler.NAME_MOUNT_SPLIT + "0");
		joints.add(joint6);

		car.setJointList(joints);

		return car.jsonify();*/
	}

}
